# JesterClaw Server — Python Requirements (Linux Ubuntu + CUDA)
# Team Lapanic / EmolOrbit
#
# Backend: llama.cpp GGUF (via llama-cpp-python)
# STT: faster-whisper (voice input → text)
# NO TTS — text output only
#
# Install PyTorch CUDA for Whisper:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Install llama-cpp-python with CUDA:
#   CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python --no-cache-dir
#
# Or just run: bash dependency_installer.bash

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets
python-multipart

# GGUF inference (llama.cpp Python bindings)
# Installed separately with CUDA flag — see dependency_installer.bash
# llama-cpp-python

# HuggingFace hub (download only)
huggingface_hub

# STT (voice → text on server)
faster-whisper

# Audio
soundfile
numpy
scipy

# Image (screenshots from client)
Pillow

# Utilities
psutil
python-dotenv
aiofiles
httpx
rich
